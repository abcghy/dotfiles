- defaults:
    link:
        relink: true

- link:
    # every folder in dot_config, symlink those folder to .config
    # This is universal
    ~/.config/:
        glob: true
        path: dot_config/**

    # Every file in home is prefix with ".", so this is fine
    ~/:
        glob: true
        path: home/.*

  # ~/.my-emacs.d:
  #   path: emacs
  # ~/Library/Rime:
  #   glob: true
  #   path: Rime/*
  #   if: '[ "$(uname -s)" = "Darwin" ]'
  # ~/.local/share/fcitx5/rime:
  #   glob: true
  #   path: Rime/*
  #   if: '[ "$(uname -s)" = "Linux" ]'

# Linux Only
- link:
    ~/.config/:
        glob: true
        path: dot_config_linux/**
        if: '[ "$(uname -s)" = "Linux" ]'
